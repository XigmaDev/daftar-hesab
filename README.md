# ریالو — PWA آفلاین مدیریت تراکنش‌های بانکی

یک Progressive Web App کاملاً آفلاین برای دریافت، استخراج و مدیریت تراکنش‌های بانکی
از روی پیامک، با رابط کاربری کاملاً فارسی (RTL) و طراحی الهام‌گرفته از Material Design 3.

**تمام داده‌ها فقط داخل مرورگر/گوشی (IndexedDB) ذخیره می‌شوند. هیچ API یا سرور بیرونی
در این پروژه وجود ندارد و هیچ درخواست شبکه‌ای برای داده‌های کاربر ارسال نمی‌شود.**

> **نکته:** یک بخش «درباره ما» در تنظیمات اضافه شده که نام، نسخه و لینک گیت‌هاب را
> نشان می‌دهد. لینک گیت‌هاب فعلاً یک آدرس نمونه است — قبل از انتشار، مقدار
> `GITHUB_URL` در ابتدای فایل `www/js/app.js` را با آدرس واقعی ریپوی خودتان
> جایگزین کنید.

---

## ۱. اجرای سریع (تست در مرورگر / نصب PWA)

فایل‌های وب برنامه داخل پوشه‌ی `www/` هستند (این ساختار عمداً این‌طور است تا
`webDir` در `capacitor.config.json` مقدار معتبری داشته باشد — نگاه کنید به
بخش ۳). چون PWA باید از طریق HTTP سرو شود (نه باز کردن مستقیم فایل)، یک سرور
ساده محلی راه بیندازید:

```bash
cd persian-bank-pwa/www
python3 -m http.server 8080
# یا
npx serve .
```

سپس در گوشی اندروید، آدرس `http://<آی‌پی‌کامپیوتر>:8080` را در Chrome باز کنید و از منوی
سه‌نقطه گزینه‌ی «افزودن به صفحه اصلی / نصب برنامه» را انتخاب کنید. برنامه به‌صورت کامل
آفلاین نصب و اجرا می‌شود (Service Worker تمام فایل‌های لازم را از پیش کش می‌کند).

برای دیپلوی واقعی، کافی است محتوای پوشه‌ی `www/` (نه کل ریپو) را روی هر هاست
استاتیک (GitHub Pages، Netlify، یا حتی یک وب‌سرور محلی روی خود گوشی) قرار دهید
— هیچ Backend لازم نیست.

## ۲. نکته‌ی مهم درباره‌ی دریافت خودکار پیامک

مرورگرها (و در نتیجه یک PWA خالص) به دلایل امنیتی اجازه‌ی دسترسی به پیامک‌های دریافتی
گوشی را نمی‌دهند. برای همین در این نسخه:

- صفحه‌ی **«افزودن تراکنش»** یک حالت **«استخراج از متن پیامک»** دارد: متن پیامک را
  پیست می‌کنید و موتور Regex (`js/smsParser.js`) اطلاعات را استخراج می‌کند — دقیقاً
  همان منطقی که در دریافت خودکار هم استفاده می‌شود.
- برای **دریافت کاملاً خودکار** پیامک، این PWA باید با **Capacitor** به یک اپ بومی
  اندروید بسته‌بندی شود. فایل‌های لازم در `android-native-sample/` آماده است، با
  یک نکته‌ی مهم که در تست‌های اولیه مشخص شد و اصلاح شده:

  **چرا یک گیرنده‌ی «پویا» به‌تنهایی کافی نیست؟** اگر برنامه فقط از یک
  `BroadcastReceiver` که داخل کد Kotlin و در زمان اجرا ثبت می‌شود
  (`context.registerReceiver(...)`) استفاده کند، این گیرنده **فقط تا وقتی پردازش
  برنامه زنده است** کار می‌کند. به‌محض اینکه اندروید برنامه را در پس‌زمینه ببندد
  (که در گوشی‌های سامسونگ/شیائومی و... خیلی زود اتفاق می‌افتد)، آن گیرنده از بین
  می‌رود و پیامک‌های بعدی اصلاً دریافت نمی‌شوند — همان چیزی که در تست شما اتفاق افتاد.

  برای همین این پروژه از **دو لایه** استفاده می‌کند:

  1. **`SmsBroadcastReceiver.kt`** — یک گیرنده‌ی **ایستا** که در `AndroidManifest.xml`
     ثبت می‌شود. اندروید این را حتی وقتی برنامه کاملاً Kill شده هم برای یک لحظه
     بیدار می‌کند تا پیامک را تحویل بگیرد. چون در آن لحظه صفحه‌ی وب برنامه اجرا
     نیست، پیامک را در یک صف محلی (`SharedPreferences`) ذخیره می‌کند و یک اعلان
     نمایش می‌دهد. **این لایه‌ی اصلی و ضروری است.**
  2. **`SmsReceiverPlugin.kt`** — افزونه‌ی Capacitor که (الف) مجوزها را می‌گیرد،
     (ب) یک گیرنده‌ی پویا هم دارد که فقط یک «زنگ خبر» بی‌محتوا به جاوااسکریپت
     می‌فرستد (خودش دیگر پیامک را استخراج نمی‌کند)، و (ج) متد `getPendingSms()`
     را دارد که صفِ ذخیره‌شده توسط لایه‌ی اول را می‌خواند. سمت جاوااسکریپت
     (`js/app.js`) این متد را در سه حالت صدا می‌زند: ابتدای اجرا، هر بار برنامه
     از پس‌زمینه به foreground برمی‌گردد، و با کمی تأخیر بعد از هر «زنگ خبر».

     **چرا این‌طور طراحی شد؟** نسخه‌ی اول، گیرنده‌ی پویا را طوری نوشته بود که خودش
     مستقل از گیرنده‌ی ایستا، پیامک را می‌خواند و مستقیماً در دیتابیس می‌نوشت — یعنی
     دو مسیر کاملاً جدا برای یک کار مشابه. نتیجه‌ی این تناقض دقیقاً همان چیزی بود که
     در تست واقعی دیده شد: گیرنده‌ی ایستا اعلان را نشان می‌داد (چون او همیشه کار
     می‌کند) ولی مسیر مستقل گیرنده‌ی پویا برای نوشتن در دیتابیس شکست می‌خورد و
     تراکنش هرگز در کارتابل ظاهر نمی‌شد. الان فقط **یک** مسیر برای نوشتن در دیتابیس
     وجود دارد (`getPendingSms` → `drainPendingNativeSms` در app.js)، و گیرنده‌ی
     پویا فقط باعث می‌شود آن مسیر سریع‌تر (بدون نیاز به بستن/باز کردن برنامه)
     اجرا شود.

  ### روش پیشنهادی: ساخت خودکار APK با GitHub Actions

  یک workflow آماده در `.github/workflows/build-android.yml` قرار دارد که تمام
  مراحل دستی زیر (افزودن پلتفرم اندروید، کپی فایل‌های Kotlin، فعال‌سازی Kotlin
  در Gradle، پچ کردن AndroidManifest، و **ثبت پلاگین در MainActivity**) را
  خودش و به‌صورت خودکار انجام می‌دهد و در پایان یک APK قابل نصب می‌سازد.

  کافی است این ریپو را در گیت‌هاب push کنید؛ workflow با هر پوش به شاخه‌ی
  `main`، با پوش کردن یک تگ نسخه (مثلاً `v1.0.0` — که یک Release رسمی هم
  می‌سازد)، یا دستی از تب Actions اجرا می‌شود. APK ساخته‌شده هم به‌عنوان
  Artifact قابل دانلود است و هم (اگر تگ زده باشید یا نه) در بخش Releases
  ریپو قرار می‌گیرد.

  **امضای نسخه‌ی Release (اختیاری):** بدون تنظیم، workflow یک APK نوع Debug
  می‌سازد که برای نصب مستقیم روی گوشی خودتان کاملاً کافی است. اگر نسخه‌ی
  امضاشده (Release) می‌خواهید، این چهار Secret را در تنظیمات ریپو (Settings ←
  Secrets and variables ← Actions) اضافه کنید: `KEYSTORE_BASE64` (فایل
  keystore با `base64` انکود شده)، `KEYSTORE_PASSWORD`، `KEY_ALIAS`،
  `KEY_PASSWORD`.

  **نکته‌ی مهم درباره‌ی این workflow:** نسخه‌ی اولیه‌ای که ممکن است جایی دیده
  باشید، یک باگ ظریف داشت — `registerPlugin` را طوری درج می‌کرد که یا اصلاً
  به MainActivity اضافه نمی‌شد (چون Capacitor نسخه‌های جدید به‌صورت پیش‌فرض
  اصلاً متد `onCreate` را override نمی‌کند) یا در جای اشتباه (بعد از
  `super.onCreate`) قرار می‌گرفت — که همان چیزی است که باعث می‌شد
  `window.Capacitor.Plugins.SmsReceiver` در برنامه‌ی نصب‌شده همیشه `undefined`
  بماند، دقیقاً مثل مشکلی که در تست دستی این پروژه هم پیش آمد. نسخه‌ی فعلی این
  فایل هر سه حالت ممکن (Java با/بدون onCreate از قبل، و Kotlin با/بدون بدنه‌ی
  کلاس) را پوشش می‌دهد و روی هرکدام تست شده است.

  ### روش دستی (Android Studio، برای توسعه و دیباگ محلی)

  مراحل بسته‌بندی (قدم‌به‌قدم و قابل‌تأیید — چون در تست واقعی مشخص شد که ثبت
  دستی پلاگین در MainActivity راحت از قلم می‌افتد، اینجا با دستور `find` هم
  مسیرها را پیدا می‌کنیم هم نتیجه را تأیید می‌کنیم):

  ```bash
  npm install
  npx cap add android

  # ۱) اول ببینید مسیر پکیج واقعی شما کجاست (باید ir/local/daftartarakonesh باشد،
  #    مگر اینکه appId را در capacitor.config.json عوض کرده باشید):
  find android -name "MainActivity.*"
  #   → مثلاً چاپ می‌کند:
  #   android/app/src/main/java/ir/local/daftartarakonesh/MainActivity.java

  # ۲) هر سه فایل Kotlin را به همان پوشه کپی کنید:
  cp android-native-sample/SmsBroadcastReceiver.kt android-native-sample/SmsReceiverPlugin.kt \
     android/app/src/main/java/ir/local/daftartarakonesh/

  # ۳) فایل MainActivity.java موجود را با نسخه‌ی آماده جایگزین کنید
  #    (اگر پروژه‌ی شما MainActivity.kt دارد نه .java، از MainActivity.kt استفاده کنید):
  cp android-native-sample/MainActivity.java android/app/src/main/java/ir/local/daftartarakonesh/MainActivity.java

  # ۴) مجوزها + تگ <receiver> داخل AndroidManifest-additions.xml را دستی به
  #    android/app/src/main/AndroidManifest.xml اضافه کنید (داخل <manifest> و <application>)

  # ۵) تأیید نهایی قبل از Build — این خط باید در MainActivity دیده شود:
  grep -r "registerPlugin(SmsReceiverPlugin" android/app/src/main/java/
  #   اگر هیچ‌چیز چاپ نشد، یعنی مرحله‌ی ۳ درست انجام نشده و باز هم
  #   window.Capacitor.Plugins.SmsReceiver در برنامه undefined خواهد بود.

  npx cap sync android
  npx cap open android   # ساخت APK/AAB نهایی در Android Studio
  ```

  **نکته‌ی مهم درباره‌ی `npx cap add android` تکراری:** اگر پوشه‌ی `android/` را
  قبلاً ساخته‌اید و دوباره `npx cap add android` را اجرا کنید، ممکن است
  `MainActivity` بازنویسی و ویرایش شما پاک شود. برای Build‌های بعدی فقط از
  `npx cap sync android` استفاده کنید (این دستور کاری با MainActivity ندارد).

  این مرحله نیازمند Android Studio و ساخت روی دستگاه واقعی است و در این محیط قابل
  Build و تست خودکار نیست؛ کد به‌صورت مرجع کامل، مستند و با لاگ‌گذاری (`Log.d`) برای
  عیب‌یابی ارائه شده است.

  ### چک‌لیست عیب‌یابی اگر باز هم پیامک دریافت نشد

  **ابزار داخل خودِ برنامه (اول از همه این را امتحان کنید، نیازی به کامپیوتر/adb نیست):**
  در تنظیمات ← بخش «عیب‌یابی دریافت پیامک» دو دکمه اضافه شده:
  - **«بررسی وضعیت اتصال به افزونه»** — اگر toast بگوید افزونه پیدا نشد، یعنی
    `registerPlugin` در `MainActivity` انجام نشده (برو مرحله‌ی ۱ زیر). اگر خطای
    مجوز داد، یعنی OS اجازه‌ی SMS را نداده (برو مرحله‌ی ۲).
  - **«خواندن دستی صف پیامک‌های در انتظار»** — این دقیقاً همان مسیری را اجرا
    می‌کند که باید بعد از رسیدن پیامک به‌صورت خودکار اجرا شود. اگر بعد از رسیدن
    اعلان این دکمه را بزنید و بگوید «۰ پیامک در صف پیدا شد»، یعنی مشکل در سمت
    Kotlin (گیرنده‌ی ایستا) است، نه در جاوااسکریپت — مستقیم برو مرحله‌ی ۳ و ۴.
    اگر عدد درستی نشان داد ولی هنوز در کارتابل نبود، مشکل جای دیگری است — به ما
    بگویید دقیقاً همین عدد و پیام toast چه بود.

  اگر این دو کافی نبود، به همین ترتیب یکی‌یکی بررسی کنید:

  1. **آیا افزونه اصلاً ثبت شده؟** اگر دکمه‌ی «بررسی وضعیت اتصال» می‌گوید افزونه
     پیدا نشد، یعنی `registerPlugin(SmsReceiverPlugin.class)` در `MainActivity`
     اجرا نشده — رایج‌ترین علتش این است که آن دو خط اصلاً به `MainActivity`
     اضافه نشده (یک قدم دستی که خیلی راحت از قلم می‌افتد). ساده‌ترین و
     مطمئن‌ترین راه‌حل: فایل آماده‌ی `MainActivity.java` (یا `.kt`) را از
     `android-native-sample/` کپی و جایگزین فایل موجود پروژه‌تان کنید (نگاه کنید
     به بخش «مراحل بسته‌بندی» بالا)، سپس این دستور را اجرا کنید تا مطمئن شوید
     واقعاً جایگزین شده:
     ```bash
     grep -r "registerPlugin(SmsReceiverPlugin" android/app/src/main/java/
     ```
     اگر چیزی چاپ نشد، فایل در مسیر اشتباه کپی شده یا `npx cap add android` را
     بعد از ویرایش دوباره اجرا کرده‌اید (که MainActivity را بازنویسی می‌کند).
     برای بررسی دقیق‌تر، در Chrome روی گوشی از طریق `chrome://inspect` به WebView
     برنامه وصل شوید و در Console بنویسید: `window.Capacitor.Plugins.SmsReceiver`
     — باید یک آبجکت برگرداند، نه `undefined`.
  2. **آیا مجوز واقعاً داده شده؟** در گوشی: تنظیمات ← اپ‌ها ← ریالو ← مجوزها
     ← پیامک، باید «مجاز» باشد. اگر باکس درخواست مجوز اصلاً باز نشد، مرحله‌ی ۱ را
     دوباره چک کنید.
  3. **آیا `AndroidManifest.xml` واقعاً به‌روزرسانی شده؟** در Android Studio از منوی
     Build ← Analyze APK می‌توانید `AndroidManifest.xml` نهایی داخل APK را ببینید و
     مطمئن شوید تگ `<receiver>` با نام کامل `ir.local.daftartarakonesh.SmsBroadcastReceiver`
     در آن هست.
  4. **لاگ‌ها را ببینید.** با اتصال گوشی به کامپیوتر و اجرای
     `adb logcat | grep -E "Daftar|chromium"` می‌توانید هم خط‌های بومی
     (`DaftarSmsReceiver`, `DaftarSmsPlugin`) و هم لاگ‌های جاوااسکریپت (`[SMS]`
     که از طریق `console.log`/`console.error` چاپ می‌شوند و WebView آن‌ها را با
     برچسب `chromium` به logcat می‌فرستد) را ببینید. اگر با ارسال پیامک هیچ خط
     `DaftarSmsReceiver`ای چاپ نشد، یعنی حتی گیرنده‌ی ایستا هم صدا زده نمی‌شود
     (برگردید به مرحله‌ی ۳). اگر `DaftarSmsReceiver` چاپ شد ولی `[SMS]` در سمت
     جاوااسکریپت چاپ نشد، مشکل در ارتباط پلاگین با WebView است (برگردید به مرحله‌ی ۱).
  5. **تست واقعی انجام دهید، نه فرضی.** بهترین تست: برنامه را کاملاً ببندید (از
     لیست اپ‌های اخیر هم حذف کنید)، از یک شماره‌ی دیگر یک پیامک بفرستید، چند ثانیه
     صبر کنید، سپس برنامه را باز کنید — باید پیامک در کارتابل ظاهر شود. اگر فقط وقتی
     برنامه باز است پیامک می‌رسد ولی بسته که هست نه، یعنی گیرنده‌ی ایستا کار
     نمی‌کند (برگردید به مراحل ۳ و ۴).
  6. **بهینه‌سازی باتری گوشی را خاموش کنید.** در گوشی‌های سامسونگ/شیائومی/هواوی و
     مشابه، تنظیمات ← باتری ← بهینه‌سازی باتری ← ریالو را روی «بدون محدودیت»
     بگذارید؛ در غیر این صورت سیستم‌عامل حتی گیرنده‌ی ایستا را هم ممکن است به‌تأخیر
     بیندازد یا مسدود کند.
  7. **اگر قصد انتشار در Google Play را دارید**، توجه کنید که `RECEIVE_SMS`/`READ_SMS`
     مجوزهای حساس (Sensitive Permissions) هستند و گوگل برای تأییدشان فرم توضیح
     مورد استفاده (Permissions Declaration Form) در Play Console می‌خواهد؛ برای
     نصب مستقیم (Sideload) روی گوشی خودتان این محدودیتی ایجاد نمی‌کند.

## ۳. ساختار پروژه

```
persian-bank-pwa/
├── www/                        تمام فایل‌های وب PWA (این پوشه = webDir در Capacitor)
│   ├── index.html                صفحه اصلی، اسپلش، درخواست مجوز اولیه، قفل PIN
│   ├── manifest.json             Web App Manifest (نصب PWA)
│   ├── sw.js                     Service Worker (کش کامل برای اجرای آفلاین)
│   ├── css/
│   │   └── style.css             سیستم طراحی (رنگ، تایپوگرافی، کامپوننت‌ها، تم تیره/روشن)
│   ├── js/
│   │   ├── db.js                   لایه دیتابیس (IndexedDB) — Repository Pattern
│   │   ├── smsParser.js            تشخیص بانک + استخراج اطلاعات با Regex
│   │   ├── charts.js                نمودار خطی و دایره‌ای با SVG خام (بدون کتابخانه)
│   │   ├── seed.js                  داده نمونه برای تست اولیه
│   │   ├── xlsxExport.js            تولید فایل واقعی Excel (.xlsx) از صفر — بدون کتابخانه‌ی بیرونی
│   │   └── app.js                   Router + رندر تمام صفحات + منطق برنامه
│   └── icons/                    آیکون‌های PWA (192، 512، maskable)
├── package.json / capacitor.config.json   بسته‌بندی اختیاری برای اندروید (webDir: "www")
├── .github/workflows/
│   └── build-android.yml       ساخت خودکار APK با GitHub Actions (نگاه کنید به بخش ۲)
└── android-native-sample/      کد مرجع دریافت پیامک در پس‌زمینه (Capacitor/Kotlin)
    ├── SmsBroadcastReceiver.kt       گیرنده‌ی ایستا — حتی وقتی برنامه Kill شده کار می‌کند
    ├── SmsReceiverPlugin.kt          پل Capacitor بین اندروید و app.js + صف pending
    ├── MainActivity.java             نسخه‌ی آماده برای Build دستی (workflow خودش این را خودکار انجام می‌دهد)
    ├── MainActivity.kt               همان، فقط اگر پروژه‌ی شما Kotlin-based است
    └── AndroidManifest-additions.xml مجوزها و ثبت گیرنده‌ی ایستا
```

## ۴. طراحی دیتابیس (IndexedDB)

| Store          | فیلدهای کلیدی                                                                 | ایندکس‌ها |
|-----------------|-------------------------------------------------------------------------------|-----------|
| `transactions`  | bankName, type, amount, balanceAfter, cardNumber, accountNumber, trackingCode, accountId, date, time, rawText, status, unitId, categoryId, description, tags[], createdAt | status, date, bank, account, unit, category, type, amount, trackingCode |
| `banks`         | name, color                                                                    | name (unique) |
| `accounts`      | bankId, title, accountType, cardNumber, accountNumber, iban, initialBalance   | bank, card, accountNumber |
| `units`         | name, color                                                                    | name (unique) |
| `categories`    | name, unitId, color                                                           | name, unit |
| `tags`          | name                                                                           | name (unique) |
| `settings`      | key → value (تک‌رکوردی: theme, pin, smsEnabled, notifEnabled, onboarded)      | keyPath: key |
| `backups`       | createdAt (ابرداده‌ی فایل‌های پشتیبان تهیه‌شده)                              | date |

روابط: هر `transaction` به‌صورت اختیاری به یک `unit`، یک `category` و یک `account`
متصل می‌شود (Foreign Key منطقی، بدون Constraint سخت‌گیرانه تا حذف واحد/دسته/حساب باعث
از دست رفتن تراکنش نشود). هر `account` به یک `bank` متصل است. **موجودی فعلی هر حساب
ذخیره نمی‌شود، بلکه همیشه به‌صورت پویا از `initialBalance` + مجموع تراکنش‌های متصل به
همان `accountId` محاسبه می‌شود** — این یعنی هیچ‌وقت موجودی و تراکنش‌ها ناهم‌خوان
(Out of sync) نمی‌شوند.

## ۵. موتور تشخیص پیامک

`js/smsParser.js` شامل فهرست بانک‌های ایرانی (ملت، ملی، صادرات، تجارت، سپه، بلو، سامان،
پاسارگاد، آینده، پارسیان، شهر، کشاورزی، رفاه، اقتصاد نوین، سینا، دی، کارآفرین، رسالت،
مهر ایران، ایران زمین، گردشگری، مسکن، پست بانک، انصار، حکمت ایرانیان و ...) به همراه
الگوهای Regex چندلایه برای مبلغ، مانده، شماره کارت/حساب، کد پیگیری، تاریخ و ساعت است.

چون فرمت پیامک بین بانک‌ها — و حتی بین نسخه‌های مختلف سامانه‌ی پیامکی یک بانک — کاملاً
متفاوت است (مثلاً بعضی بانک‌ها به‌جای «مبلغ: ۱۰۰۰۰۰ ریال» می‌نویسند «برداشت:۱۰۰۰۰۰» یا حتی
فقط «۱۰۰۰۰۰-» بدون هیچ کلیدواژه‌ای)، این موتور برای هر فیلد چند الگوی جایگزین را به ترتیب
اولویت امتحان می‌کند:

- **مبلغ**: ابتدا الگوی «مبلغ: عدد»، سپس «برداشت:عدد/واریز:عدد»، سپس عدد همراه «ریال/تومان»
  در هر جای متن، و در نهایت عدد به‌تنهایی که بلافاصله علامت `+`/`-` دارد.
- **نوع تراکنش**: از کلمات کلیدی («برداشت»، «واریز»، «خرید»، «پرید» و...)، و اگر هیچ‌کدام
  نبود، از علامت `+`/`-` چسبیده به مبلغ حدس زده می‌شود.
- **تاریخ**: چند فرمت را پشتیبانی می‌کند — کامل با جداکننده‌ی `/`، `-` یا `.` (شامل ارقام
  فارسی مثل «۱۴۰۵.۰۴.۲۴»)، کوتاه بدون سال («۱/۴»)، و فشرده بدون جداکننده بلافاصله قبل از
  ساعت («۱۱۲۸-۲۳:۵۸»). برای جلوگیری از قاطی‌شدن با شماره‌حساب‌های خط‌تیره‌دار، اولویت با
  الگویی است که به سال شمسی (۱۳xx/۱۴xx) شبیه‌تر باشد.
- **حروف عربی**: چون بعضی سامانه‌های بانکی از حروف عربی «ي» و «ك» به‌جای فارسی «ی» و «ک»
  استفاده می‌کنند (مثلاً «بانک ملي» یا «بانك صادرات»)، متن قبل از تشخیص یکسان‌سازی می‌شود.

اگر مبلغ یا بانک همچنان تشخیص داده نشود، فیلد `recognized: false` برمی‌گردد و کاربر
می‌تواند در فرم افزودن تراکنش، مقادیر را دستی تکمیل کند. برای پشتیبانی از فرمت‌های خاص و
جدید، کافی است الگوی Regex مربوطه در همین فایل گسترش داده شود.

## ۶. دسته‌بندی سریع در کارتابل

برای اینکه بررسی تراکنش‌های تازه از پیامک تا حد امکان سریع باشد، با ضربه روی هر تراکنش
در **کارتابل**، به‌جای باز شدن فرم کامل، یک شیت کوچک باز می‌شود که در آن:

- واحد و دسته‌بندی به‌صورت چیپ‌های قابل لمس (تک‌انتخابی) نمایش داده می‌شوند — یک ضربه کافی است.
- اگر حسابی ثبت کرده باشید، یک ردیف «حساب» هم نمایش داده می‌شود. اگر پیامک شماره
  کارت یا حساب داشته باشد (اکثر بانک‌ها)، حساب مربوطه خودکار پیش‌انتخاب می‌شود؛
  ولی بعضی بانک‌ها (مثلاً بلو) شماره حساب را داخل پیامک نمی‌فرستند — برای این
  موارد یک هشدار کوچک نشان داده می‌شود و کاربر می‌تواند حساب را دستی با یک ضربه
  انتخاب کند.
- اگر واحد/دسته‌بندی مدنظر هنوز وجود ندارد، با دکمه‌ی «+ جدید» می‌توان همان‌جا و بدون خروج
  از شیت، آن را ساخت و بلافاصله انتخاب کرد.
- با دکمه‌ی «ثبت و انتقال به تراکنش‌ها»، تراکنش دسته‌بندی شده، وضعیتش «بررسی‌شده» می‌شود و
  از کارتابل خارج می‌شود.
- دکمه‌ی «ویرایش کامل تراکنش» هم برای زمانی است که نیاز به اصلاح مبلغ، توضیحات یا سایر
  جزئیات باشد.

فشار طولانی (Long Press) روی هر تراکنش هم مثل قبل حالت انتخاب چندتایی را برای حذف/ویرایش
گروهی فعال می‌کند.

## ۷. امکانات پیاده‌شده

- **مدیریت بانک‌ها و حساب‌ها** (تنظیمات ← بانک‌ها و حساب‌ها): افزودن هر تعداد بانک
  (از فهرست بانک‌های ایرانی یا نام دلخواه)، به‌همراه رنگ اختصاصی؛ برای هر بانک می‌توان
  چند حساب/کارت با عنوان، نوع حساب (جاری/پس‌انداز/کارت/ارزی)، شماره کارت، شماره حساب،
  شماره شبا و **موجودی اولیه** ثبت کرد. موجودی فعلی هر حساب به‌صورت خودکار از
  «موجودی اولیه + مجموع تراکنش‌های متصل به همان حساب» محاسبه می‌شود.
- داشبورد با موجودی، جمع واریز/برداشت به تفکیک بازه (امروز/هفته/ماه/سال/کل)، بخش
  «موجودی هر حساب» (در صورت ثبت بانک/حساب)، نمودار روند ۷ روز اخیر، نمودار دایره‌ای
  دسته‌بندی، تراکنش‌های اخیر.
- کارتابل تراکنش‌های جدید با وضعیت «جدید» تا زمان بررسی کاربر.
- لیست تراکنش‌ها با جستجوی پیشرفته (متن پیامک، مبلغ، شماره کارت/حساب، توضیحات) و فیلتر
  بر اساس وضعیت/واحد/دسته‌بندی.
- انتخاب چندتایی با فشار طولانی (Long Press) + حذف گروهی + ویرایش گروهی.
- صفحه جزئیات تراکنش با متن کامل پیامک، تمام فیلدهای استخراج‌شده، حساب متصل، حذف تکی.
- افزودن/ویرایش تراکنش (دستی یا با پیست پیامک) با امکان اتصال به یک حساب مشخص؛ هنگام
  استخراج از پیامک، در صورت تطابق شماره کارت/حساب با یکی از حساب‌های ثبت‌شده، حساب
  به‌صورت خودکار پیشنهاد می‌شود.
- مدیریت واحدها و دسته‌بندی‌ها (ایجاد/ویرایش/حذف/رنگ اختصاصی).
- گزارش‌ساز با فیلتر ترکیبی و خروجی **Excel (.xlsx واقعی)**، CSV، JSON و چاپ/PDF
  (از طریق چاپ مرورگر). فایل Excel با یک تولیدکننده‌ی OOXML/ZIP نوشته‌شده از صفر
  در `js/xlsxExport.js` ساخته می‌شود — بدون هیچ کتابخانه‌ی بیرونی یا CDN، کاملاً
  همسو با اصل آفلاین‌بودن کامل برنامه؛ خروجی با ابزارهایی مثل openpyxl تست و
  تأیید شده و در Excel/Google Sheets/LibreOffice به‌درستی باز می‌شود (راست‌به‌چپ
  برای متن فارسی هم فعال است).
- تنظیمات: تم روشن/تیره، رمز عبور ۴ رقمی (PIN)، فعال/غیرفعال‌سازی دریافت پیامک و اعلان.
- پشتیبان‌گیری کامل (Export/Import JSON — شامل بانک‌ها و حساب‌ها) و پاک‌سازی کل داده‌ها.
- نصب PWA + اجرای کاملاً آفلاین با Service Worker.

## ۸. محدودیت‌های شناخته‌شده / گام‌های بعدی

- دریافت *خودکار* پیامک فقط در بسته‌ی Capacitor فعال می‌شود (بخش ۲).
- ورود با اثر انگشت (WebAuthn) در این نسخه پیاده نشده؛ قفل PIN جایگزین آن است. برای
  افزودن اثر انگشت واقعی در اپ بسته‌بندی‌شده، از افزونه‌ی `@capacitor/biometric-auth`
  می‌توان استفاده کرد.
- تاریخ‌های شمسی به‌صورت متن خام (همانی که در پیامک آمده) ذخیره می‌شوند؛ برای محاسبات
  دقیق «این هفته/این ماه شمسی» می‌توان یک کتابخانه‌ی تقویم جلالی افلاین اضافه کرد.
- الگوهای Regex برای فرمت‌های خاص و نادر پیامک ممکن است نیاز به تنظیم دقیق‌تر داشته
  باشند؛ ساختار کد برای افزودن الگوی جدید بسیار ساده است (`js/smsParser.js`).
